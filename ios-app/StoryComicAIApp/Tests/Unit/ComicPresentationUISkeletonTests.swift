import XCTest

final class ComicPresentationUISkeletonTests: XCTestCase {
    func testGenerationToRevealFlowSkeleton() throws {
        throw XCTSkip(
            "UI test bundle is not configured yet. Skeleton reserved for: generation complete -> reveal -> book preview -> flat reader -> export."
        )
    }
}
